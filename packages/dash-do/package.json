{
	"name": "@firmly/dash-do",
	"version": "0.39.1",
	"private": true,
	"type": "module",
	"main": "src/index.js",
	"scripts": {
		"dev": "wrangler dev",
		"build-ci": "echo '{}' > data.json",
		"deploy-dev": "npm run build-ci && wrangler deploy --env dev",
		"deploy-ci": "npm run build-ci && wrangler deploy --env ci",
		"deploy-qa": "npm run build-ci && wrangler deploy --env qa",
		"deploy-uat": "npm run build-ci && wrangler deploy --env uat",
		"deploy-prod": "npm run build-ci && wrangler deploy --env prod",
		"format-code": "eslint . --fix",
		"format-code:check": "eslint . --fix=false",
		"test": "npm run build-ci && vitest run --silent",
		"test:watch": "npm run build-ci && vitest",
		"test:ui": "npm run build-ci && vitest --ui"
	},
	"dependencies": {
		"foundation": "github:goyalu/foundation#*"
	},
	"devDependencies": {
		"@babel/eslint-parser": "^7.28.5",
		"@babel/plugin-syntax-import-assertions": "^7.27.1",
		"@cloudflare/vitest-pool-workers": "^0.11.1",
		"eslint-plugin-import": "^2.32.0",
		"vitest": "~3.1.4",
		"wrangler": "^4.14.1"
	}
}
