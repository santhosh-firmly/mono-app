/**
 * Pagination constants for the session record service
 */

/**
 * Default number of items to display per page in lists
 */
export const DEFAULT_ITEMS_PER_PAGE = 7;

/**
 * Minimum items threshold to show pagination controls
 */
export const PAGINATION_THRESHOLD = 7;
